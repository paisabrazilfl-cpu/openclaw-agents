# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files (NEVER commit real keys)
.env
.env*.local

# runtime keystore written by the Settings UI (NEVER commit)
.openclaw-secrets.json

# debug / misc
*.log
.DS_Store
*.tsbuildinfo
next-env.d.ts
